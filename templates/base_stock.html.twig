<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <!-- CSS only -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-wEmeIV1mKuiNpC+IOBjI7aAzPcEZeedi5yW5f2yOq55WWLwNGmvvx4Um1vskeMj0"
      crossorigin="anonymous"
    />

    <title>{% block title %}Welcome!{% endblock %}</title>
    {# Run `composer require symfony/webpack-encore-bundle` and uncomment the
    following Encore helpers to start using Symfony UX #} {% block stylesheets
    %} {#{{
      encore_entry_link_tags("app")
    }}#} {% endblock %} {% block javascripts %} {#{{
      encore_entry_script_tags("app")
    }}#} {% endblock %}
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container">
        <a class="navbar-brand" href={{path('cart_validate')}}>Stock</a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarNav"
          aria-controls="navbarNav"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
           
            <li class="nav-item">
              <a class="nav-link" href={{path('item_index')}}>Items</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href={{path('category_index')}}>Categories</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href={{path('app')}}>clientèle</a>
            </li>
          </ul>
        </div>
      </div>
    </nav>
    <div class="container">{% block body %}{% endblock %}</div>
    <!-- JavaScript Bundle with Popper -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-p34f1UUtsS3wqzfto5wAAmdvj+osOnFyQFpp4Ua3gs/ZVWx6oOypYoCJhGGScy+8"
      crossorigin="anonymous"
    ></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js" integrity="sha512-894YE6QWD5I59HgZOGReFYm4dnWc1Qt5NtvYSaNcOP+u1T9qYdvdihz0PPSiiqn/+/3e7Jo4EaG7TubfWGUrMQ==" crossorigin="anonymous"></script>

<script type="text/javascript">
    $(".counter_plus").on("click",function(){
      const id = $(this).data('id')
      const elem = $("#input"+id)
      const value = parseInt(elem.val()) + 1
    
      elem.attr('value',value)
    });
    $(".counter_moins").on("click",function(){
      const id = $(this).data('id')
      const elem = $("#input"+id)
      const value = parseInt(elem.val()) - 1
      if(value < 0) return
      elem.attr('value',value)
    });
    $(".submit").on("click",function(){
      const id = $(this).data('id')
      const elem = $("#"+id)
      elem.submit()
    });
    
</script>

  </body>
</html>
