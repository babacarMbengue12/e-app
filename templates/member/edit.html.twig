{% extends "base_client.html.twig" %} {% block body %}
<h1>Edit Member</h1>

{{ include("member/_form.html.twig", { button_label: "Update" }) }}

<a class="btn btn-info" href="{{ path('member_index') }}">back to list</a>

{{ include("member/_delete_form.html.twig") }}
{% endblock %}
