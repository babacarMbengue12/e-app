{% extends "base_stock.html.twig" %} {% block body %}
<h1>Create new Item</h1>

{{ include("item/_form.html.twig") }}

<a class="btn btn-info" href="{{ path('item_index') }}">back to list</a>
{% endblock %}
